name: DEBUG SSH KEY

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Show RAW secret (FIRST & LAST line only)
        run: |
          echo "---- RAW SECRET KEY (first 3 lines) ----"
          echo "${{ secrets.KEY }}" | head -3
